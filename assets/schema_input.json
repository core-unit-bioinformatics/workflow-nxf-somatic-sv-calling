{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/core-unit-bioinformatics/somaticsvcalling/main/assets/schema_input.json",
    "title": "core-unit-bioinformatics/somaticsvcalling pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "status": {
                "type": "string",
                "errorMessage": "Status can only be normal or tumor. Defaults to normal, if none is supplied.",
                "meta": ["status"],
                "default": "normal",
                "enum": ["tumor", "normal"]
            },
            "bam": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.bam$",
                "errorMessage": "BAM file for tumor sample must be provided, cannot contain spaces and must have extension '.bam'"
            }
        },
        "required": ["sample", "status", "bam"]
    }
}
